extends ../layout

block content

  if obj
    table.table.table-striped
    thead
    tbody
      for restaurant in obj
        div.restaurantHeader
          h5= restaurant.name
          h5= ['firstname' + ', ' + 'lastname']
        
